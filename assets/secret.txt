Some fun facts about GDScript:
1. The `assert` function is useless outside the editor.
2. Errors don't show any information outside the editor.
3. The keyword `class_name` will cause similar namespacing issues as in C.
4. Mixing classes and static functions will throw strange namespacing errors.

